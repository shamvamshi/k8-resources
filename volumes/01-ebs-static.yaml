apiVersion: v1
kind: PersistentVolume
metadata:
  name: ebs-static
spec:
  accessModes:
  - ReadWriteOnce
  capacity:
    storage: 20Gi
  csi:
    driver: ebs.csi.aws.com # driver installation
    fsType: ext4              # file system type
    volumeHandle: vol-02b2d4106f272d807